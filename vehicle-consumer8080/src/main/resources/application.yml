server:
  port: 8080

eureka:
  client:
    # true表示向注册中心注册自己
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/

logging:
  level:
    com.yongda.service.OrderConsumerService: debug
    com.yongda.service.AuthConsumerService: debug